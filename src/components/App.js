import React from 'react';

import ExampleContainer from '../containers/ExampleContainer';

export default () => {
	return (
		<div>
			Hello, I'm react running in a chrome extension option page
			<ExampleContainer />
		</div>
	);
};
